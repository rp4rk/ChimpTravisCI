'use strict';

module.exports = function() {
    this.Given(/^I visit "([^"]*)"$/, url => {

    });

    this.And(/^I enter "([^"]*)" into the search bar$/, phrase => {

    });
    
    this.When(/^I press the search button$/, () => {

    });
};
